PORT=3002
NODE_ENV=development

# Comma-separated CORS allowlist
ALLOWED_ORIGINS=http://localhost:5173,http://127.0.0.1:5173

# Admin login
ADMIN_USERNAME=ben
# SHA-512 hash of admin password
ADMIN_PASSWORD_HASH=

# Optional agent token authentication (set one)
AGENT_TOKEN_HASH=
# AGENT_TOKEN=

# Session expiration in hours
SESSION_TTL_HOURS=24

# Comma-separated command allowlist for cron jobs
CRON_ALLOWED_BINARIES=echo,date,uptime
