services:
  mission-control:
    build:
      context: .
      dockerfile: Dockerfile.dev
    volumes:
      - .:/app
      - /app/node_modules # Anonymous volume to prevent local node_modules from overwriting container's
    ports:
      - "5173:5173"
    environment:
      - VITE_WATCH_USE_POLLING=true
    healthcheck:
      test: [ "CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:5173" ]
      interval: 10s
      timeout: 5s
      retries: 5
  # Placeholder for your Agent container
  # agent:
  #   image: your-agent-image
  #   volumes:
  #     - .:/code # The agent needs access to the same files to edit them
  #   depends_on:
  #     - mission-control
  #   environment:
  #     - APP_URL=http://mission-control:5173
